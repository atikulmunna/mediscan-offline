package com.mediscan.offline.domain

enum class CapturePanelType(val label: String) {
    PacketDateSide("Packet Date Side"),
    PacketDetailSide("Packet Detail Side"),
    Strip("Strip"),
    Other("Other"),
}
